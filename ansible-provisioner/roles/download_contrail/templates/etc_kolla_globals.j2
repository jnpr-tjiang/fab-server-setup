kolla_internal_vip_address: "{{ all_in_one_node }}"
kolla_external_vip_address: "{{ all_in_one_node }}"
contrail_api_interface_address: "{{ all_in_one_node }}"
network_interface: "{{ interface_name }}"
kolla_external_vip_interface: "{{ interface_name }}"

neutron_opencontrail_init_image_full: "{{ container_registry }}/contrail-openstack-neutron-init:{{ contrail_version }}"
nova_compute_opencontrail_init_image_full: "{{ container_registry }}/contrail-openstack-compute-init:{{ contrail_version }}"

openstack_release: "4.0.0"
neutron_plugin_agent: "opencontrail"
enable_haproxy: "no"
